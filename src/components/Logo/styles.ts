import styled from 'styled-components'

export const LogoStyle = styled.img`
  width: auto;
  height: auto;
  border-radius: 8px;

  @media (max-width: 768px) {
    padding-top: 16px;
    display: flex;
  }
`
